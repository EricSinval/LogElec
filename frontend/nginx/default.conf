server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index/home.html;

    location = / {
        return 302 /index/home.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}